<view class="page">
  <view class="card">
    <text class="title">当前接口地址</text>
    <text class="value">{{currentBaseUrl}}</text>
    <text class="hint">当前环境：{{envVersion}}。真机演示建议提前准备一个可访问的局域网地址或云端地址。</text>
  </view>

  <view class="card">
    <text class="title">快速预设</text>
    <view class="preset-list">
      <view class="preset-item" wx:for="{{presets}}" wx:key="key" bindtap="onSelectPreset" data-url="{{item.url}}">
        <view class="preset-head">
          <text class="preset-name">{{item.name}}</text>
          <text class="preset-url">{{item.url}}</text>
        </view>
        <text class="preset-desc">{{item.desc}}</text>
      </view>
    </view>
  </view>

  <view class="card">
    <text class="title">自定义地址</text>
    <input
      class="input"
      placeholder="请输入 http://IP:8080/api 或 https://域名/api"
      value="{{customBaseUrl}}"
      bindinput="onCustomInput"
    />
    <text class="hint">支持在陌生网络环境下直接改地址，无需重新改代码发版。</text>
  </view>

  <view class="actions">
    <button class="btn ghost" bindtap="handleTest" disabled="{{testing}}">{{testing ? '测试中...' : '测试连接'}}</button>
    <button class="btn primary" bindtap="handleSave" disabled="{{saving}}">{{saving ? '保存中...' : '保存并使用'}}</button>
  </view>

  <view class="actions single">
    <button class="btn probe" bindtap="handleAutoProbe" disabled="{{probing}}">{{probing ? '探测中...' : '自动探测局域网服务器'}}</button>
  </view>

  <view class="actions single">
    <button class="btn light" bindtap="handleReset">恢复默认地址</button>
  </view>

  <view class="card">
    <text class="title">演示建议</text>
    <text class="tip-line">1. 同一 Wi-Fi 演示时，填电脑当前 IPv4 地址，例如 `http://192.168.x.x:8080/api`。</text>
    <text class="tip-line">2. 跨网络或无法控制局域网时，优先准备固定外网接口地址。</text>
    <text class="tip-line">3. 如果接口改了，先在这里“测试连接”，再开始演示。</text>
  </view>
</view>
